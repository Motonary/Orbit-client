version: '3.7'

services:
  orbit-client:
    container_name: orbit-api
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - '4000:4000'
      - '6000:6000'
    environment:
      - NODE_ENV=development
